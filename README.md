# OrbitDine

OrbitDine is now a focused, frontend-only customer ordering experience.

## What is included
- A single professional customer order screen.
- A curated list of dummy menu items.
- Quantity controls and a live order summary with totals.

## Run locally
```bash
npm install
npm run dev
```

Then open the Vite URL shown in your terminal.
